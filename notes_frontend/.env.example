# Frontend environment variables for the notes app.
# IMPORTANT: Do not commit real secrets to version control.

# Base URL for the backend REST API (notes_database). Example: https://api.example.com
VITE_API_BASE_URL=/api
